#include "Player.h"

Player::Player(sf::IntRect rect, sf::Vector2f scale, sf::Vector2f position) : Entity(rect, scale, position)
{

}

//void Player::draw(sf::RenderTarget& target, sf::RenderStates states) const
//{
//
//}
